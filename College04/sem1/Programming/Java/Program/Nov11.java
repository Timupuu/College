import java.util.Scanner;
/**
 * Write a description of class Nov11 here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class Nov11
{
    public static void main(String[] args)
    {
        Scanner scanner = new Scanner(System.in);
        
        // Simple take name and output
        /** String name;
        System.out.println("Enter a name:");
        name = scanner.nextLine();
        System.out.println("Hello " + name);    
         */
        
        //Simple calculator
        /**
        double a;
        double b;
        char opr;
        double output;
        System.out.println("Enter to the simple calculator");
        scanner.nextLine();
        System.out.println("Enter the 1st digit");
        a = scanner.nextDouble();
        System.out.println("Enter the 2nd digit");
        b = scanner.nextDouble();
        
        System.out.println("Enter the operator: (*,/,+,-)");
        opr = scanner.next().charAt(0);
        
        if (opr == '*'){
            output = a*b;
            System.out.println("The output is " + output);
        }
        else if (opr == '/'){
            output = a/b;
            System.out.println("The output is " + output);
        }
        else if (opr == '+'){
            output = a+b;
            System.out.println("The output is " + output);
        }
        else if (opr == '-'){
            output = a-b;
            System.out.println("The output is " + output);
        }
        */
       
   }
}
